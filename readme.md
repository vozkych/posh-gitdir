# Git directory listing - PowerShell module

The PowerShell module which brings a new directory listing command with Git status for each list item.

## Details

Run the Get-Help command:
```powershell
Get-Help Get-GitChildItem
```
